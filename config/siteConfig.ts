type SiteConfig = {
	name: string;
	description: string;
	url: string;
};

export const siteConfig: SiteConfig = {
	name: 'Remote Kitchen',
	description: 'Your very own place of cooking food.',
	url: 'http://localhost:3000',
};
